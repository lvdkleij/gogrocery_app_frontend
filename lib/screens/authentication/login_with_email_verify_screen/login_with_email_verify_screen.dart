import 'package:flutter/material.dart';

class LoginWithEmailVerifyScreen extends StatelessWidget {
  const LoginWithEmailVerifyScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Material(child: Center(child: Text('Login with email')));
  }
}
